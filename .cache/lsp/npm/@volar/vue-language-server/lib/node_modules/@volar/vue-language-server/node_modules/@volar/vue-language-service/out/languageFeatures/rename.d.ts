import * as vscode from 'vscode-languageserver-protocol';
import type { LanguageServiceRuntimeContext } from '../types';
import { VueDocuments } from '../vueDocuments';
export declare function register(context: LanguageServiceRuntimeContext): (uri: string, position: vscode.Position, newName: string) => Promise<vscode.WorkspaceEdit | undefined>;
export declare function mergeWorkspaceEdits(original: vscode.WorkspaceEdit, ...others: vscode.WorkspaceEdit[]): void;
/**
 * TODO: rewrite this
 *
 * Start from Script LS
 * -> Access all results
 *
 * Start from template LS
 * -> Start from template content?
 *    -> Access all results
 * -> Start from script content?
 *    -> Yes: Only access template results
 *    -> No: Access all results
 */
export declare function embeddedEditToSourceEdit(tsResult: vscode.WorkspaceEdit, vueDocuments: VueDocuments): vscode.WorkspaceEdit | undefined;
